# 憲法

pistachio-talk のすべてのエントリ・すべての貢献者（人間・AI問わず）が従う5つの条文。
**憲法は厳格に。それ以外（文体・粒度・運用の細部）は CONTRIBUTING.md にゆだね、軽量に保つ。**

これらは `scripts/validate.mjs` によって機械的に検査され、CI でブロックされる。「なんとなく守る」ではなく「守らないとマージできない」仕組みで実効性を担保する。

---

## 第1条: 出典URL必須（絶対規範）

すべてのエントリ（`phrases/`）は、以下の**3点テスト**を満たす出典URLを最低1つ持たなければならない。

1. そのページにフレーズが **原語で** 記載されている（`phrase`）
2. フレーズの **意味の説明** がある（`meaning`）
3. **使用例または使用場面の記述** がある（`usage`）— 例文でも「バーで男性が女性に言う」のような文脈記述でもよい

1つのURLで3点を満たせない場合、**複数のURLで分担してよい**。ただし各 `sources[]` 要素に `covers: [phrase, meaning, usage]` のうち実際にカバーする項目を明記し、**全 source の和集合が3点を覆うこと**（`verification.status` が `unverified` 以外のとき機械検証される）。

**AI が出典を追加する場合、URL を実際に fetch し内容を確認してから記載する。幻覚URL（存在確認していないURL）の記載は絶対禁止。** これはリード（`leads/`）の `candidate.sources` にも及ぶ — 噂であっても捏造は許されない。

## 第2条: フィクション排除 —「起源は問わない、流通は実在せよ」

- 要件は「**現実の人々が実際に使う・使った**」こと。
- **映画のセリフ・歌詞は起源としては可**。例えば映画発の口説き文句が実際にナンパの現場で定着したケースは収録できる。ただしその場合、「現実に使われている」ことを示す出典が起源の出典とは別に必要。**映画の脚本や歌詞そのものだけを出典にしたエントリは不可。**
- `origin_type` に `folk | literary | media | commercial | unknown` のいずれかを記録する。
- 我々や AI が創作したフレーズ、出典の裏付けなく「ありそう」と生成されたフレーズは、いかなる形でも収録不可。

## 第3条: 出典の質ティア

| Tier | `sources[].type` | 定義 | 例 |
|---|---|---|---|
| T1 | `dictionary_academic` | 辞書・学術文献・言語学リソース | Wiktionary（出典付き項目）、論文、専門辞書 |
| T2 | `language_media` | 語学メディア・報道（編集プロセスあり） | 語学学習ブログ、新聞のカルチャー記事 |
| T3 | `native_personal` | ネイティブ話者による個人発信 | ネイティブのブログ・YouTube・podcast |
| T4 | `forum_ugc` | フォーラムUGC | Reddit、HiNative、Quora の回答 |

各 source に `type` と `author_is_native`（`true`/`false`/`unknown`）を記録する。

**教訓**: このプロジェクトのお手本記事（トルコ語 "Fıstık gibisin" の解説）自体が「T2・非ネイティブ執筆・語源主張に一切引用なし」という性質を持っていた。**T2 であっても鵜呑みにしない。** 語源・文化史などの強い主張には、可能な限り独立した裏取りを試みる（Stage 2 の仕事）。

## 第4条: 検証ステータスと昇格条件

| `verification.status` | 条件 | 置き場所 |
|---|---|---|
| `unverified` | 出典3点テスト未確認 | `inbox/` `leads/` のみ。**`phrases/` に進入不可**（機械検証） |
| `single_source` | 3点テストを満たす出典1系統 | `phrases/`（ドラフト扱い） |
| `cross_checked` | **独立した2出典以上**（別ドメイン・別著者）、うち1つ以上が T1–T3 | `phrases/` |
| `native_confirmed` | ネイティブ話者による確認の記録あり。確認の証跡（HiNativeでの質問と回答のURL、field-note ID 等）を `verification.evidence[]` に記載 | `phrases/` |

昇格は PR で行い、レビュアーが出典を実際に開いて確認する。降格（リンク切れ・出典の信頼性崩壊が判明）も PR で行い、履歴に理由を残す。ごまかしの昇格（出典が伴わない自己申告）は CI が第1条・第4条の検査で機械的にブロックする。

## 第5条: プライバシー

共有リポジトリ（`field-notes/` を含む）には、実践相手の**個人を特定できる情報を一切書かない**（実名・所属・特定可能な状況描写の禁止）。生々しい詳細メモは各自の `private/`（gitignore 済み）へ。

## 第6条: キザであること — 収集範囲の純化

`phrases/` に収録できるのは、以下の**キザ4要素**を最低1つ持つフレーズのみ。

| 要素 | `kiza_elements` の値 | 意味 |
|---|---|---|
| 比喩 | `metaphor` | 何かに例えて口説く・褒める（ピスタチオ、ボンボン…） |
| 機知 | `wit` | ひねり・言葉遊び・切り返しの妙 |
| 詩的イメージ | `poetic_image` | 情景・韻律の美しさで運ぶ |
| 文化的暗喩 | `cultural_allusion` | その文化圏の物語・習慣・歴史を踏まえて初めて味が出る |

「ここへよく来ますか？」のような**直接的な機能フレーズは、実在し流通していても収録対象外**。各エントリは `kiza_elements` に該当要素を明記し（機械検証）、本文「比喩の論理」等の節でその根拠を説明する。要素の申告が本文で裏付けられているかはレビューで確認する（直接フレーズを比喩と強弁しない）。

---

## 憲法の及ぶ範囲

- `phrases/` — 全条文が適用される正典
- `leads/` — 第1条（候補レベルの出典fetch確認）・第2条（フィクション排除）・第6条（fresh/open のリードは `candidate.kiza_elements` 必須）が適用される。`verification.status` の概念はまだ持たない（噂だから）
- `essences/` — 創作の素材となる歴史・文化・自然の要素。**第1条の fetch 原則が適用される**（出典URLを実際に取得・確認してから記載。幻覚URL禁止）。フレーズではないため3点テストは適用しない
- `creations/` — 料理場（Kitchen）で我々が創作したセリフ。**第2条は適用されない**（創作と明示された別棟だから）。ただし出自の記録として `recipe`（使用エッセンス）が必須であり、素材レベルでは第1条の精神が貫かれる。第5条は適用される。**creations のセリフが phrases/ に昇格することはない** — phrases/ は現実に流通する本物だけの棚である
- `inbox/` — 第1条の緩和版（source URL 1つのみ必須）。`status` は `unverified` 固定
- `field-notes/` — 第5条が適用される。対象は `phrase_id` または `creation_id` のどちらか一方
